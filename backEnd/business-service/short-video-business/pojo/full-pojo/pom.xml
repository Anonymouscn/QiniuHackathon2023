<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <artifactId>full-pojo</artifactId>
    <name>full-pojo</name>

    <parent>
        <groupId>cn.net.anonymous</groupId>
        <artifactId>pojo</artifactId>
        <version>1.0.0</version>
    </parent>

    <dependencies>
        <!-- file pojo -->
        <dependency>
            <groupId>cn.net.anonymous</groupId>
            <artifactId>file-pojo</artifactId>
            <version>1.0.0</version>
        </dependency>
        <!-- log pojo -->
        <dependency>
            <groupId>cn.net.anonymous</groupId>
            <artifactId>log-pojo</artifactId>
            <version>1.0.0</version>
        </dependency>
        <!-- post pojo -->
        <dependency>
            <groupId>cn.net.anonymous</groupId>
            <artifactId>post-pojo</artifactId>
            <version>1.0.0</version>
        </dependency>
        <!-- sms pojo -->
        <dependency>
            <groupId>cn.net.anonymous</groupId>
            <artifactId>sms-pojo</artifactId>
            <version>1.0.0</version>
        </dependency>
        <!-- user pojo -->
        <dependency>
            <groupId>cn.net.anonymous</groupId>
            <artifactId>user-pojo</artifactId>
            <version>1.0.0</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- build for maven -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven-build.version}</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <encoding>${maven-build.encoding}</encoding>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>